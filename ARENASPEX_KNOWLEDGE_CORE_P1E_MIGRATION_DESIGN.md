# ArenaSpex Knowledge Core P1E — تصميم هجرة مراجع المجال الأول

الحالة: **reviewed_not_activated**. هذه المرحلة تصمم وتصنف وتحاكي فقط؛ لا تكتب في قاعدة البيانات ولا تغيّر خطة محفوظة أو مرجعًا إنتاجيًا.

## 1. القرار التنفيذي

الهجرة المستقبلية تُقسم إلى عمليتين مستقلتين: هجرة المرجع المملوك للمنصة، ثم هجرة اختيارية لخطة الأستاذ. لا يسمح بأي كتابة إلا عند ثبوت السلالة والهوية والبنية، مع حفظ النص والعدد والترتيب والحقول التي عدلها الأستاذ. تطابق النص وحده ليس دليل ملكية.

## 2. خط أساس P1D

تم تدقيق 35 مرجعًا تشغيليًا: 10 `ALIAS`، و4 `REVIEWED_MAP`، و14 `REPLACE_LATER`، و7 `PRODUCT_DECISION_REQUIRED`. السنة الثانية محلولة 7/7؛ التعارضات الحقيقية باقية في السنوات 3–5.

## 3. حدود الهجرة

- داخل النطاق: المجال الأول، السنوات 3–5، مرجع المنصة الافتراضي، ومحاكاة خطط على شكل `TeacherLearningPlan`.
- خارج النطاق: Prisma، API، UI، التوزيع السنوي، `ClassPlannedSession`، الكراس اليومي، مذكرة الحصة، التنقيط والتقييم.
- عدد أهداف الأستاذ وترتيبها وصياغتها ومواضع الإدماج التي غيّرها تبقى محفوظة.

## 4. هجرة المرجع مقابل هجرة خطة الأستاذ

1. **REFERENCE MIGRATION:** إصدار مرجع جديد، بيان versioned، اختبارات، وتفعيل مستقل قابل للتراجع.
2. **TEACHER PLAN MIGRATION:** عملية لاحقة اختيارية ذات فحص ملكية وتاريخ تنفيذ، وسجل تدقيق مستقل. لا تبدأ تلقائيًا عند تفعيل المرجع.

## 5. جدول البدائل الأربعة عشر

كل صف أدناه `APPROVED_REPLACEMENT` على مستوى المرجع، لكنه آمن مستقبلًا فقط لافتراض منصة غير معدل. `variantId = null`، والمتطلبات تؤخذ صراحة من `ObjectiveConcept` المشار إليه في البيان البرمجي.

| المستوى | المرجع | المعنى القديم المختصر | ObjectiveConcept البديل | الصياغة البديلة |
|---|---|---|---|---|
| س3 | `__2` | وضعيات جسمية وتوازن ثابت | `lvl_p3:...:1` | ينتقل من المشي إلى الهرولة بتحكم وتدرج |
| س3 | `__3` | انتقال سريع بين وضعيات | `lvl_p3:...:2` | ينتقل من الهرولة إلى الجري الخفيف وفق الموقف |
| س3 | `__4` | تنقلات اتجاهية | `lvl_p3:...:3` | يتدرج من الجري الخفيف إلى الجري السريع |
| س3 | `__6` | تغيير اتجاه ومسارات | `lvl_p3:...:4` | ينفذ الرمي من الثبات بيد واحدة في مجال آمن |
| س3 | `__8` | ربط وضعيات وتنقلات | `lvl_p3:...:6` | يربط عمليات الجري والرمي وفق الموقف |
| س3 | `__9` | مسار توازن وتنقل | `lvl_p3:...:7` | ينجز جملة مترابطة وآمنة من الجري والرمي |
| س4 | `__2` | وضعيات توازن | `lvl_p4:...:1` | يضبط وضعية الجسم وآلية الخطوة أثناء الجري |
| س4 | `__3` | انتقال بين وضعيات | `lvl_p4:...:2` | ينسق الأطراف أثناء الجري بسرعة متزايدة |
| س4 | `__4` | تنقلات اتجاهية | `lvl_p4:...:3` | يجري على محور مستقيم مع ترابط الحركة |
| س4 | `__8` | توازن وتنقل مركب | `lvl_p4:...:6` | يكيف وتيرة جريه وينسجم مع المجموعة |
| س4 | `__9` | مسار وضعيات وتوازن | `lvl_p4:...:7` | يربط حركات الجري فرديًا وجماعيًا |
| س5 | `__4` | مسارات مستقيمة ومنحنية | `lvl_p5:...:3` | يتخذ وضعية متوازنة أثناء الوثب وينجز سقوطًا سليمًا |
| س5 | `__8` | توازن وتنقل وتحكم | `lvl_p5:...:6` | يركب الجري والوثب والرمي بانسيابية وتحكم |
| س5 | `__9` | ابتكار مسار مركب | `lvl_p5:...:7` | ينجز جملة رياضية مترابطة مع الآخرين |

الهوية الكاملة، النص الكامل، المتطلبات، والدليل موجودة في `DOMAIN_ONE_LEGACY_P1E_MIGRATION_MANIFEST` لتجنب ازدواج مصدر الحقيقة.

## 6. الحالات السبع غير المحسومة

هي: س3/`__7`، س4/`__6`، س4/`__7`، س5/`__2`، س5/`__3`، س5/`__6`، س5/`__7`. لا تصبح alias أو reviewed mapping قبل إجابات المالك الواردة في `ARENASPEX_P1E_PRODUCT_DECISIONS.md`.

## 7. ملكية الحقول

| الحقل | الملكية | سياسة المستقبل |
|---|---|---|
| `id` | PLATFORM_REFERENCE | PRESERVE |
| `sourceReferenceId` | PLATFORM_REFERENCE | REMAP_ID_ONLY |
| صياغة الهدف، العدد، الترتيب | TEACHER_OWNED | PRESERVE |
| المكونات | MIXED_OR_CONTEXTUAL | MERGE_NON_DESTRUCTIVELY |
| محتوى التعلم/المعارف/التوجيهات | MIXED_OR_CONTEXTUAL | PRESERVE |
| محتوى الإنجاز، الوضعيات، الوسائل، الملاحظات | TEACHER_OWNED | PRESERVE |
| موضع الإدماج | MIXED_OR_CONTEXTUAL | REVIEW_REQUIRED |
| السجل المنفذ | HISTORICAL_SNAPSHOT | PRESERVE |

## 8. كاشف الافتراضي مقابل محتوى الأستاذ

الكاشف يقارن: المعرف الثابت، `sourceReferenceId`، صف P1D، موضع الهدف، البنية التاريخية المعروفة، ثم الحقول كلًا على حدة. نتائجه: `PLATFORM_DEFAULT_UNMODIFIED`، أو `PLATFORM_DEFAULT_TEXT_EDITED`، أو `PLATFORM_DEFAULT_STRUCTURALLY_EDITED`، أو `TEACHER_CREATED`، أو `UNKNOWN_OR_AMBIGUOUS`. لا توجد مطابقة ضبابية ولا سلطة لتشابه النص.

## 9. كاشف خطأ الإدماجية الثانية

يصنف `LEGACY_DEFAULT_INTEGRATION_PLACEMENT` فقط عندما تكون الإدماجية الثانية مثبتة بعد الهدف قبل الأخير، وتطابق هويتها وبنيتها النسخة التاريخية المعروفة، ولم يتغير العدد أو الترتيب. أي موضع آخر عدله الأستاذ يُحفظ؛ الشك ينتقل للمراجعة.

## 10. سياسة التاريخ المنفذ

`EXECUTED_HISTORY_PRESERVE` يعني وجود `ClassPlannedSession` أو NotebookEntry أو LessonPlan أو سجل تقييم مرتبط. عندها لا يسمح المحرك بأي كتابة مستقبلية على الخطة أو اللقطات التاريخية. يمكن فقط دراسة alias مرجعي لا يغير معنى السجل الماضي، وبمراجعة مستقلة.

## 11. بنية المحاكاة

المحرك دالة حتمية بلا I/O. يأخذ خطة/مجالًا، كتالوجًا، نسخة افتراضية تاريخية، واعتماديات تاريخية اختيارية. يعيد تصنيف الخطة والأهداف والإدماجيات، الهوية قبل/بعد، الدليل، الإجراء المقترح، الحقول المحفوظة والتحذيرات. لا يستورد Prisma أو خدمة API ولا يحفظ شيئًا.

## 12. نتائج المحاكاة

- الافتراضي القديم للسنوات 3–5: البدائل المعتمدة مرشحة مستقبلًا، والحالات السبع تبقى للمراجعة.
- تعديل النص: اقتراح `REMAP_ID_ONLY` مع حفظ النص.
- إضافة وضعية أو ملاحظة، هدف خاص، تغيير العدد أو الترتيب: محفوظة.
- تحريك الإدماجية يدويًا: محفوظ.
- fallback التاريخي غير المعدل: مرشح إصلاح.
- وجود تاريخ منفذ: يمنع الكتابة.
- هوية غامضة: مراجعة مطلوبة.
- خطة مصالحة بالفعل: `NO_ACTION`، وإعادة الفحص متطابقة.

## 13. محاكاة التغطية قبل/بعد

يستخدم المحرك المحول الدلالي ومحرك التغطية القائمين على نسخة مستنسخة في الذاكرة. قبل المصالحة تبقى خطط 3–5 غير mapped. بعد تطبيق المقترحات الآمنة فقط قد ترتفع التغطية، لكن الحالات غير المحسومة تبقى معلنة. ارتفاع 4/4 لا يُستخدم دليلًا على صحة mapping.

## 14. بيان الهجرة

المعرف `knowledge-core:migration:p1e-domain1-legacy-v1`، من `legacy-algerian-curriculum-domain1` إلى `knowledge-core:v1.2-domain1-grades2-5`. الحالة `reviewed_not_activated`. يحتوي 14 بديلًا و7 قرارات مؤجلة وقاعدة إصلاح الإدماجية وسياسات الملكية والتاريخ.

## 15. Idempotency

الهوية الدلالية المطابقة في خطة مصححة تنتج `NO_ACTION`. المحاكاة لا تعدل المدخل، وإعادتها بالمدخل نفسه تعيد النتيجة نفسها.

## 16. تصميم التراجع

قبل أي P1F: لقطة كاملة قبل الهجرة، marker بإصدار الهجرة لكل خطة، سجل before/after وسبب القرار، transaction لكل خطة، توقف كامل عند الفشل الجزئي، وإعادة تشغيل تتجاوز `NO_ACTION`. حد التراجع هو خطة الأستاذ؛ السجلات المنفذة لا تدخل في الكتابة أصلًا. استرجاع اللقطة يعيد فقط حقول المرجع التي كتبها الإصدار، ولا يمس حقول الأستاذ.

## 17. المخاطر

أهم المخاطر: نسب ملكية خاطئ بسبب تشابه النص، تفسير ترتيب مخصص كافتراضي، توسيع معنى الحالات الجزئية، أو تغيير سجل منفذ. الحماية: هوية وسلالة ثابتتان، مقارنة بنسخة تاريخية، قرار منتج صريح، ووقف الكتابة عند التاريخ أو الغموض.

## 18. قرارات المنتج

سبعة أسئلة نعم/لا موثقة في ملف القرارات. لا يجوز تفعيل P1F قبل تثبيت كل جواب كـ `APPROVE_MAPPING` أو `APPROVE_REPLACEMENT` أو `SPLIT_REQUIRED` أو `PRESERVE_LEGACY` مع إصدار مرجعي مراجع.

## 19. شروط الانتقال إلى P1F

1. اعتماد القرارات السبعة.
2. تجميد release جديد وhash للكتالوج.
3. تحديد مصدر النسخة التاريخية لكل generator lineage.
4. تصميم سجل audit وmarker ونسخة rollback.
5. اختبار على snapshot منزوعة الهوية، ثم مراجعة نتائج dry-run يدويًا.
6. موافقة صريحة منفصلة على هجرة المرجع وعلى هجرة الخطط.

## 20. نطاق P1F المقترح

ابدأ بهجرة **المرجع فقط** وإصدار canonical جديد بلا تعديل خطط. بعد مراقبة ذلك، نفذ أداة dry-run على الخطط وأخرج تقرير مراجعة. اجعل الكتابة الاختيارية مرحلة مستقلة لا تقبل إلا `SAFE_*` و`LEGACY_DEFAULT_INTEGRATION_PLACEMENT` غير المرتبط بتاريخ، وتحفظ النص والعدد والترتيب والحقول المملوكة للأستاذ.
