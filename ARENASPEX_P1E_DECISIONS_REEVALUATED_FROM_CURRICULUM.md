# ArenaSPEX P1E Decisions Re-evaluated from the 2023 Curriculum

Status: audit only. The P1E manifest remains `reviewed_not_activated`; none of these recommendations activates a mapping or changes a Teacher plan.

| Case | Previous proposal | Official evidence and correct domain | Semantic comparison | New recommendation |
|---|---|---|---|---|
| G3/`f_locomotion__7` | Product decision; candidate was graded light→fast running | G3/D1 includes walk→jog→light→fast progression; G3/D2 separately specifies slow/medium/fast paces. PDF p.17–18. | “Changing speed according to situation” overlaps both, but does not establish the official D1 progression and may represent D2 pace execution. | `MOVE_TO_OTHER_DOMAIN` for the legacy meaning, or replace in D1 with the official progression. Do not alias. |
| G4/`__6` | Product decision; curve-running candidate | G4/D1 explicitly lists maximal running on a curve and body/limb control. PDF p.19–20. | Legacy curved-path locomotion is underspecified but the official D1 target is clear. | `APPROVE_REPLACEMENT` with “يضبط جسمه وأطرافه أثناء الجري على منحنى”; preserve Teacher wording in existing plans. |
| G4/`__7` | Product decision; group pace candidate | G4/D1 explicitly names running within a group and running pace; components include coexistence with the group. PDF p.19–20. | Legacy speed change omits synchronization/group context. | `APPROVE_REPLACEMENT`; not alias, because approving an alias would add group meaning. |
| G5/`__2` | Product decision; jumping balance/safe landing candidate | G5/D1 explicitly states balance and safe landing during jumping. PDF p.22–23. | Generic static/dynamic balance is broader and not equivalent to jump-specific posture. | `APPROVE_REPLACEMENT`; preserve legacy/Teacher text unless an explicit reset is chosen. |
| G5/`__3` | Product decision; running posture candidate | G5/D1 FC/components explicitly require adapting body posture to different situations. PDF p.22–23. | Legacy transition with harmony/balance is source-compatible at component level, but not equivalent to the current run-specific candidate. | `SPLIT_REQUIRED`: create/refine a posture-adaptation concept; do not map directly to running posture. |
| G5/`__6` | Product decision; jump/landing candidate | Obstacles are not stated in G5/D1; G5/D2 gives jumping/clearance technique and coordinated jump phases. PDF p.22–24. | Obstacle movement may imply jumping but cannot prove D1 posture identity. | `MOVE_TO_OTHER_DOMAIN` (D2) or preserve legacy pending a D2 concept; reject a D1 alias. |
| G5/`__7` | Product decision; transition among run/jump/throw | G5/D1 explicitly names all three and suitable transition/propulsion; G5/D2 also contains their technical execution. PDF p.22–24. | Legacy includes locomotion and jumping but omits throwing; direct alias would overstate meaning. | `APPROVE_REPLACEMENT` with the D1 run/jump/throw transition concept; do not alias. |

## Decisions required before P1F

1. Confirm moving G3/`__7` legacy meaning to a future D2 review rather than preserving it in D1.
2. Approve source-backed replacements for G4/`__6`, G4/`__7`, G5/`__2`, and G5/`__7`.
3. Approve creation/refinement of a G5/D1 posture-adaptation concept for G5/`__3`.
4. Confirm moving G5/`__6` obstacle meaning to D2 or preserving it unmapped.
5. Confirm that all actions affect future reference identity only; existing Teacher wording, order, count and history remain protected.
